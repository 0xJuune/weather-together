import React from 'react'

function Weather(props) {
    
  // api call, using the location being passed into Weather()  

  
  
  return (
    <div>
        {/* render cards here by passing weather data to card components? */}
    </div>
  )
}

export default Weather